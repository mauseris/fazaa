# Sample .env (placeholders only) - DO NOT commit real keys
# Copy to .env and fill the real values locally

# Recommended (governance) gateway
GATEWAY_BASE_URL=https://your-gateway.example.com
GATEWAY_API_KEY=YOUR_GATEWAY_API_KEY
MODEL_NAME=gpt-4o-mini
EMBED_MODEL_NAME=text-embedding-3-small

# Local Anthropic (development only)
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL_NAME=claude-sonnet-5

# Server port
PORT=3000
